version: "3"
services:
  seed:
    image: node:lts-alpine
    working_dir: "/usr/src"
    volumes:
      - ./:/usr/src/seed
    entrypoint: >
      sh -c "npx @angular/cli new --skipInstall --skipGit && 
             mv ./*/* /usr/src/seed"
